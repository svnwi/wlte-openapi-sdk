[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "wlte-openapi-local"
version = "0.2.0"
description = "WLTE OpenAPI Python SDK"
readme = "README.md"
requires-python = ">=3.9"
license = { text = "Apache-2.0" }
authors = [
  { name = "SVNWI", email = "support@svnwi.com" }
]
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["wlte_openapi"]
